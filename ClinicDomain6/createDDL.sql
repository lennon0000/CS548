CREATE TABLE PATIENT (ID BIGINT NOT NULL, BIRTHDATE DATE, NAME VARCHAR(255), PATIENTID BIGINT, PRIMARY KEY (ID))
CREATE TABLE TREATMENT (ID BIGINT NOT NULL, DTYPE VARCHAR(31), DIAGNOSIS VARCHAR(255), TTYPE VARCHAR(2), patient_id BIGINT, provider_npi BIGINT, DOSAGE FLOAT, NAME VARCHAR(255), TYPE VARCHAR(255), DATE DATE, PRIMARY KEY (ID))
CREATE TABLE PROVIDER (NPI BIGINT NOT NULL, NAME VARCHAR(255), PRIMARY KEY (NPI))
CREATE TABLE RADDATE (ID BIGINT NOT NULL, DATE DATE, radiology_fk BIGINT, PRIMARY KEY (ID))
ALTER TABLE TREATMENT ADD CONSTRAINT FK_TREATMENT_treatments_fk FOREIGN KEY (treatments_fk) REFERENCES PROVIDER (id)
ALTER TABLE TREATMENT ADD CONSTRAINT FK_TREATMENT_provider_npi FOREIGN KEY (provider_npi) REFERENCES PROVIDER (NPI)
ALTER TABLE TREATMENT ADD CONSTRAINT FK_TREATMENT_patient_id FOREIGN KEY (patient_id) REFERENCES PATIENT (ID)
ALTER TABLE RADDATE ADD CONSTRAINT FK_RADDATE_radiology_fk FOREIGN KEY (radiology_fk) REFERENCES TREATMENT (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
